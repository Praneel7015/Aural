[phases.setup]
nixPkgs = ["python311", "gcc", "libsndfile"]

[phases.install]
cmds = ["pip install -r requirements-light.txt"]

[start]
cmd = "uvicorn main_light:app --host 0.0.0.0 --port ${PORT:-8000}"
